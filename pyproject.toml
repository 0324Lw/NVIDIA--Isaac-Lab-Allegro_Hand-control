[project]
name = "allegro-hand-isaaclab-rl"
version = "0.1.0"
description = "Educational pure-RL baseline tasks for Allegro Hand control in NVIDIA Isaac Lab."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
